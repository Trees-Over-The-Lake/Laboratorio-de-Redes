\begin{document}
    

% Vídeo usado de exemplo para o server multithread
% https://www.youtube.com/watch?v=xceTFWy_eag

% Como mencionado nessa \emph{thread} de discução do \emph{stackoverflow}. Para fazer dual-stacking ipv4 e ipv6
% https://stackoverflow.com/questions/65877630/how-to-bind-both-ipv4-and-ipv6-source-address-to-python-socket
%     addr = ("", 8080)  # all interfaces, port 8080
% if socket.has_dualstack_ipv6():
%     s = socket.create_server(addr, family=socket.AF_INET6, dualstack_ipv6=True)
% else:
%     s = socket.create_server(addr)
\end{document}
